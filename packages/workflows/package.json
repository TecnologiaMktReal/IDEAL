{
  "name": "@ideal/workflows",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"lint workflows pending\"",
    "test": "echo \"test workflows pending\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ideal/domain": "workspace:*"
  }
}
