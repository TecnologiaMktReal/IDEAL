{
  "name": "@ideal/reports",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"lint reports pending\"",
    "test": "echo \"test reports pending\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
